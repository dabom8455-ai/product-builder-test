# ⚙️ 고객 대시보드 복제(프로비저닝) 실제 절차 — 셋대리 (수동판)

> 자산 (나) · 클부장 2026-09-12 · G7 전략(첫 고객~5명 수동 복제)의 실행 절차.
> 대상 스택: dohyeon-piano (React19+Vite6+TS, Netlify 자동배포, Netlify Functions/Blobs, DASHBOARD_PIN, 노션 백업).
> **핵심 아이디어:** 고객마다 **별도 Netlify 사이트**로 배포 → env·Blobs 저장소가 사이트 단위로 격리 = **데이터 자연 격리**. 코드 하나, 사이트 여럿.

---

## 0. 준비물 (온보딩 폼에서 수령)
학원명 / 원하는 사이트명 / 수업 타임 / 수강료·결제일 / 입금계좌 / (선택) 원생 명단 / 노션 사용여부.

## 1. 사이트 생성 (테넌트 = 별도 사이트)
1. [ ] 동일 코드베이스(master)를 고객용 **새 Netlify 사이트**로 연결. (같은 repo → New site from Git, 또는 사이트 복제)
2. [ ] 사이트 이름/서브도메인 지정: 예) `학원명-dohyeon.netlify.app`.
3. [ ] 커스텀 도메인 원하면 연결(선택).

## 2. 환경변수 세팅 (고객별 고유값 — 격리의 핵심)
Netlify → Site settings → Environment variables:
- [ ] `DASHBOARD_PIN` = 고객 전용 PIN(신규 발급, 안전 채널로 전달)
- [ ] `NOTION_TOKEN` = 고객 노션 통합 토큰
- [ ] `NOTION_BACKUP_DB` = 고객 노션 백업 DB ID
> 🔒 Netlify Blobs 저장소는 **사이트 단위로 분리**되므로, 사이트를 나누면 홈페이지 콘텐츠·데이터가 고객 간 섞이지 않는다.

## 3. 배포·동작 확인
1. [ ] 배포 트리거 → 빌드 성공 확인.
2. [ ] `/dashboard` 접속 → PIN 잠금 동작 확인.
3. [ ] 자동화 3종 스모크 테스트: 상담 예약 1건(리마인드) · 진도 1건(월말 리포트 반영) · 입금 1건(미납 표시).
4. [ ] 노션 백업 1회 실행 → 고객 노션에 백업 생성 확인.

## 4. 고객 정보 반영
- [ ] 홈페이지/대시보드 기본 정보(상호·주소·전화·수업 타임)를 고객 확정본으로 입력.
- [ ] 원생 명단 이관(있으면) → 중복·오등록 검수.

## 5. 인계·기록
- [ ] Go-live 인수 확인(계약서 §4) → 납품 완료.
- [ ] 내부 웹 URL 레지스트리(`docs/infra/web_urls.md`)에 고객 인스턴스 1줄 기록.
- [ ] 성대리에게 인계(30일 체크인 예약), 보대리에게 지원 대상 등록.

---

## ✅ 코드 검증 완료 (2026-09-12 · `code_verification_2026-09-12.md`)
4항목 전부 코드로 확인 → **수동 복제로 첫 고객 세팅 가능(GO)**.
1. PIN·노션 토큰 env 주입(하드코딩 無) ✅ 2. 데이터 사이트별 격리 ✅(대시보드=브라우저 localStorage, 공유 DB 없음) 3. 다중 사이트 배포 ✅ 4. 업데이트 시 대시보드 학원정보(localStorage) 보존 ✅.

## 🟡 세팅 시 반드시 고객에 공지할 제약 2가지
- **제약 A(★): 단일 브라우저 저장** — 대시보드 데이터는 그 브라우저에만 있음. **멀티기기 동기화 없음.** 온보딩에서 "주로 쓰는 한 기기 + **자동 백업 ON**"을 반드시 안내. 브라우저 데이터 삭제=손실(복구는 노션/내보내기 수동 import).
- **제약 B: 공개 홈페이지는 코드 편집 필요(`site.ts`)** → **첫 상품은 운영 대시보드 중심**으로 판다. 공개홈은 옵션/후순위.

→ 서버 동기화·멀티테넌트는 고객 3~5명 시점 착수(⑤ 셋대리, 견인 우선).

## 다음 단계 (반자동화, 고객 3~5명 시점)
- 신규 고객 프로비저닝 스크립트(사이트 생성+env 주입 자동화).
- 우리 홈페이지에서 원장이 직접 로그인하는 **고객 포털**(진짜 멀티테넌트) 설계 착수.
